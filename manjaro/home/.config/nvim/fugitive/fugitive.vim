" Fugitive extension configuration.

" Key binding for git status command
nmap <leader>gs :Gstatus<CR>

" Key binding for git commit command
nmap <leader>gc :Gcommit<CR>

" Key binding for git push
nmap <leader>gp :Gpush<CR>
